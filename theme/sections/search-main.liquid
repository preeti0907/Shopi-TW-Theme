{% schema %}
{
  "name": "Search Results",
  "tag": "section",
  "class": "bg-white",
  "settings": []
}
{% endschema %}

<section class="py-12 px-4 max-w-7xl mx-auto">
  <h1 class="text-3xl font-bold mb-6 text-center">Search Results for “{{ search.terms }}”</h1>

  {% if search.results_count == 0 %}
    <p class="text-gray-500 text-center">No results found. Try searching for something else.</p>
  {% else %}
    <p class="text-sm text-gray-500 mb-4 text-center">
      {{ search.results_count }} result{% if search.results_count != 1 %}s{% endif %}
    </p>

    <div class="grid gap-6 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4">
      {% for item in search.results %}
        {% render 'product-card',
          product: item,
          show_secondary_image: true,
          show_add_to_cart: true,
          badge_type: 'percentage',
          title_lines: 2
        %}
      {% endfor %}
    </div>
  {% endif %}
</section>
