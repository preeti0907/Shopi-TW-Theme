{% schema %}
    {
      "name": "Main Article Content",
      "tag": "section",
      "class": "bg-white",
      "settings": []
    }
    {% endschema %}
    
    <article class="prose lg:prose-xl mx-auto py-12 px-4">
      <h1 class="text-4xl font-bold mb-4">{{ article.title }}</h1>
      <p class="text-gray-500 mb-8">{{ article.published_at | date: "%B %d, %Y" }} · {{ article.author }}</p>
      <div>
        {{ article.content }}
      </div>
    </article>
    